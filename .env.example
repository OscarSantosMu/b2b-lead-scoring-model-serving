# Environment Configuration Example
# Copy this file to .env and update with your values

# Environment
ENV=production
LOG_LEVEL=INFO

# Security
API_KEYS=demo-api-key-123,test-key-456,prod-key-789

# CORS
CORS_ORIGINS=*
HOST=0.0.0.0
PORT=8000
WORKERS=4

# Model Configuration
# Options: "local", "sagemaker", "azure"
MODEL_ENDPOINT_PROVIDER=local
MODEL_PATH=models/model.json
MODEL_VERSION=1.0.0

# AWS SageMaker Configuration (if using sagemaker)
AWS_REGION=us-east-1
SAGEMAKER_ENDPOINT_NAME=lead-scoring-endpoint
# AWS_ACCESS_KEY_ID=your-key
# AWS_SECRET_ACCESS_KEY=your-secret

# Azure ML Configuration (if using azure)
AZURE_ML_ENDPOINT_URL=https://your-endpoint.azureml.net
AZURE_ML_API_KEY=your-api-key
# AZURE_ML_DEPLOYMENT_NAME=blue  # optional
